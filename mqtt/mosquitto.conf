# Mosquitto 2.x: si no defines listener, puede arrancar en "local only mode"
# y no aceptará conexiones remotas (típico en Docker). :contentReference[oaicite:5]{index=5}

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data

log_dest file /mosquitto/log/mosquitto.log
